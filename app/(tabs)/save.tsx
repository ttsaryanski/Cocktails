import { StyleSheet, Text, View } from "react-native";

const Save = () => {
    return (
        <View>
            <Text>Save</Text>
        </View>
    );
};

export default Save;

const styles = StyleSheet.create({});
